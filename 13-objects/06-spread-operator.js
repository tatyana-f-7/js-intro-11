const numbers = [ 5, 10, 20, 15 ];

console.log(Math.max(...numbers));
console.log(Math.min(...numbers));

const batch = {
    number; 11,
    program: 'SDET'
}

const student = {
    firstName: 'Alex',
    lastName: 'Smith',
    hobbies: 
}